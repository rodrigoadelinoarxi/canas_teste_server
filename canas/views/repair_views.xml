<odoo>
    <record id="repair_form_override" model="ir.ui.view">
        <field name="name">repair.form.override</field>
        <field name="model">repair.order</field>
        <field name="inherit_id" ref="repair.view_repair_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='fleet_vehicle']" position="after">
                <field name="original_location" force_save="1" readonly="1"
                       attrs="{'invisible': [('original_location', '=', False)]}"/>
            </xpath>
            <xpath expr="//field[@name='lot_id']" position="after">
                <field name="code_equipment_related"/>
                <field name="responsible_id"/>
            </xpath>
            <xpath expr="//field[@name='operations']/tree/field[@name='product_uom_qty']" position="after">
                <field name="product_available_qty"/>
            </xpath>
            <xpath expr="//field[@name='operations']/form//field[@name='price_unit']" position="before">
                <field name="product_available_qty"/>
            </xpath>
            <xpath expr="//header/button" position="before">
                <button name="generate_purchase_order" string="Create Quotation" type="object" class="oe_highlight"
                        attrs="{'invisible': [('state', 'in', ['done', 'cancel'])]}"/>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <field name="analytic_account_active" invisible="1"/>
                <field name="allow_timesheets" invisible="1"/>
                <field name="subtask_count" invisible="1"/>
                <field name="project_id" invisible="1"/>
                <field name="task_id" invisible="1"/>
                <page string="Timesheets" attrs="{'invisible': [('allow_timesheets', '=', False)]}">
                    <group>
                        <group>
                            <field name="planned_hours" widget="float_time"/>
                            <label for="subtask_planned_hours" groups="project.group_subtask_project"
                                   attrs="{'invisible': [('subtask_count', '=', 0)]}"/>
                            <div class="o_row" groups="project.group_subtask_project"
                                 attrs="{'invisible': [('subtask_count', '=', 0)]}">
                                <field name="subtask_planned_hours" widget="float_time"/><span> planned hours</span>
                            </div>
                        </group>
                        <group>
                            <field name="progress" widget="progressbar"/>
                        </group>
                    </group>
                    <group name="timesheet_error" attrs="{'invisible': [('analytic_account_active', '!=', False)]}">
                        <div class="alert alert-warning" role="alert">
                            You can not log timesheets on this project since is linked to an inactive analytic account.
                            Please change it, or reactivate the current one to timesheet on the project.
                        </div>
                    </group>
                    <field name="timesheet_ids" mode="tree,kanban"
                           attrs="{'invisible': [('analytic_account_active', '=', False)]}"
                           context="{'default_project_id': project_id, 'default_name':'', 'default_task_id': task_id}">
                        <tree editable="bottom" string="Timesheet Activities" default_order="date">
                            <field name="date"/>
                            <field name="user_id" invisible="1"/>
                            <field name="employee_id" required="1"/>
                            <field name="name"/>
                            <field name="unit_amount" widget="timesheet_uom"/>
                            <field name="company_id" invisible="1"/>
                            <field name="task_id" invisible="1"/>
                            <field name="project_id" invisible="1"/>
                        </tree>
                        <kanban class="o_kanban_mobile">
                            <field name="date"/>
                            <field name="user_id"/>
                            <field name="employee_id"/>
                            <field name="name"/>
                            <field name="unit_amount"/>
                            <field name="project_id"/>
                            <templates>
                                <t t-name="kanban-box">
                                    <div t-attf-class="oe_kanban_card oe_kanban_global_click">
                                        <div class="row">
                                            <div class="col-6">
                                                <strong><span><t t-esc="record.employee_id.value"/></span></strong>
                                            </div>
                                            <div class="col-6 pull-right text-right">
                                                <strong><t t-esc="record.date.value"/></strong>
                                            </div>
                                        </div>
                                        <div class="row">
                                            <div class="col-6 text-muted">
                                                <span><t t-esc="record.name.value"/></span>
                                            </div>
                                            <div class="col-6">
                                                <span class="pull-right text-right">
                                                    <field name="unit_amount" widget="float_time"/>
                                                </span>
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                        <form string="Timesheet Activities">
                            <sheet>
                                <group>
                                    <field name="date"/>
                                    <field name="user_id" invisible="1"/>
                                    <field name="employee_id" required="1"/>
                                    <field name="name"/>
                                    <field name="unit_amount" string="Duration" widget="float_time"/>
                                    <field name="project_id" invisible="1"/>
                                </group>
                            </sheet>
                        </form>
                    </field>
                    <group attrs="{'invisible': [('analytic_account_active', '=', False)]}">
                        <group class="oe_subtotal_footer oe_right" name="project_hours">
                            <field name="effective_hours" widget="float_time"/>
                            <field name="subtask_effective_hours" widget="float_time"
                                   attrs="{'invisible' : [('subtask_effective_hours', '=', 0.0)]}"/>
                            <field name="total_hours_spent" widget="float_time" class="oe_subtotal_footer_separator"
                                   attrs="{'invisible' : [('subtask_effective_hours', '=', 0.0)]}"/>
                            <field name="remainqing_hours" widget="float_time" class="oe_subtotal_footer_separator"/>
                        </group>
                    </group>
                </page>
                <page string="Checklist">
                    <group>
                        <group>
                            <field name="checklist_done" widget="percentpie" nolabel="1"/>
                        </group>
                    </group>
                    <field name="task_checklist_ids" context="{'default_user_id': responsible_id}" nolabel="1">
                        <tree editable="bottom" decoration-success="state=='done'" decoration-danger="state=='cancel'">
                            <field name="sequence" widget="handle"/>
                            <field name="user_id" options="{'no_open':True,'no_create_edit': True}"/>
                            <field name="name"/>
                            <button name="action_done" string="Done" type="object" icon="fa-thumbs-up"
                                    attrs="{'invisible': [('state', 'in', ['done', 'cancel'])]}" style="color:green;"/>
                            <button name="action_cancel" string="Cancel" type="object" icon="fa-thumbs-down"
                                    attrs="{'invisible': [('state', 'in', ['cancel'])]}" style="color:red;"/>
                            <button name="action_draft" string="Draft" type="object" icon="fa-rotate-left"
                                    attrs="{'invisible': [('state', 'in', ['draft'])]}" style="color:grey;"/>
                            <field name="state"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
